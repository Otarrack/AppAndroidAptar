package com.florian.projet.model;

public class Site extends CategoryType {

    public Site(int id, double volume, int rebutInPercent, double rebutInQuantity) {
        super(id, volume, rebutInPercent, rebutInQuantity);
    }
}
